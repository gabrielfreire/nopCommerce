
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: nopcommerce-gateway
  namespace: nopcommerce
spec:
  selector:
    istio: ingressgateway # use istio default controller
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "www.ofreire.com"
    - "ofreire.com"
    - "boltz.ofreire.com"
    - "watchsix.ofreire.com"
    - "fixit.ofreire.com"
    # tls:
    #   httpsRedirect: true